<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>AniKit - Dark Premium</title>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    
    <!-- Fontes e Ícones -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        :root {
            --bg-primary: #0a0a0f;
            --bg-secondary: #15151e;
            --accent: #6d28d9;
            --text-primary: #e0e0e0;
            --text-secondary: #a1a1aa;
            --border: rgba(255,255,255,0.1);
        }

        body {
            background: var(--bg-primary);
            color: var(--text-primary);
            font-family: 'Space Grotesk', sans-serif;
            min-height: 100vh;
            line-height: 1.6;
        }

        /* Header Aprimorado */
        .glass-header {
            background: rgba(10, 10, 15, 0.95);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border);
            padding: 1rem 2rem;
            position: sticky;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .home-link {
            color: var(--text-primary);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .home-link:hover {
            background: rgba(255,255,255,0.05);
            color: var(--accent);
        }

        .user-info-container {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 8px 16px;
            background: rgba(255,255,255,0.05);
            border-radius: 50px;
            border: 1px solid var(--border);
            transition: all 0.3s ease;
        }

        .user-info-container:hover {
            background: rgba(255,255,255,0.08);
        }

        .user-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid var(--accent);
        }

        .user-name {
            max-width: 200px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            font-size: 0.95rem;
            font-weight: 500;
        }

        /* Card de Perfil Ajustado */
        .profile-card {
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.2);
            overflow: hidden;
            margin-bottom: 2rem;
            position: relative;
        }

        .avatar-wrapper {
            position: relative;
            margin: 2rem auto 1.5rem;
            width: 140px;
            height: 140px;
            border-radius: 50%;
            background: linear-gradient(45deg, var(--accent), #8b5cf6);
            padding: 4px;
            box-shadow: 0 4px 12px rgba(109,40,217,0.3);
            box-sizing: border-box;
        }

        .avatar-img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            border: 3px solid var(--bg-secondary);
            object-fit: cover;
            box-sizing: border-box;
        }

        /* Social Links Ajustados */
        .social-links {
            gap: 1rem;
            margin: 1.5rem 0;
        }

        .social-link {
            width: 42px;
            height: 42px;
            border-radius: 8px;
            background: rgba(255,255,255,0.05);
            transition: transform 0.2s ease, background 0.3s ease;
        }

        .social-link:hover {
            background: var(--accent);
            transform: translateY(-3px);
        }

        /* Formulário Aprimorado */
        .edit-form label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--text-secondary);
        }

        .edit-form .form-control {
            background: rgba(255,255,255,0.03) !important;
            border: 1px solid var(--border) !important;
            color: var(--text-primary) !important;
            border-radius: 8px;
            padding: 0.875rem 1.25rem;
            margin-bottom: 1.25rem;
            transition: all 0.3s ease;
        }

        .edit-form .form-control::placeholder {
            color: var(--text-secondary) !important;
            opacity: 0.7;
        }

        .save-btn {
            background: var(--accent);
            border: none;
            padding: 1rem 2rem;
            border-radius: 8px;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            width: 100%;
        }

        .save-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(109,40,217,0.4);
        }

        /* Responsividade Ajustada */
        @media (max-width: 768px) {
            .glass-header {
                padding: 1rem;
                flex-wrap: wrap;
                gap: 1rem;
                justify-content: center;
            }

            .home-link span {
                display: none;
            }

            .user-info-container {
                padding: 6px 12px;
            }

            .user-avatar {
                width: 42px;
                height: 42px;
            }

            .user-name {
                max-width: 150px;
                font-size: 0.9rem;
            }

            .avatar-wrapper {
                width: 120px;
                height: 120px;
                margin: 1.5rem auto;
            }
        }

        @media (max-width: 576px) {
            .profile-card {
                border-radius: 12px;
            }

            .edit-form .form-control {
                padding: 0.75rem 1rem;
            }
        }
    </style>
</head>

<body>
    <!-- Header Ajustado -->
    <header class="glass-header">
        <a href="/#" class="home-link">
            <i class="fas fa-home"></i>
            <span>Início</span>
        </a>
        
        <div class="user-info-container">
            <span class="user-name"><%= user.username %></span>
            <img src="<%= user.ft %>" alt="Avatar" class="user-avatar">
        </div>
    </header>

    <!-- Conteúdo Principal -->
    <main class="container py-5">
        <div class="row g-4">
            <!-- Seção de Perfil -->
            <div class="col-lg-4">
                <div class="profile-card p-4 text-center">
                    <div class="avatar-wrapper">
                        <img src="<%= user.ft %>" alt="Avatar" class="avatar-img">
                    </div>
                    
                    <h3 class="mt-3 mb-2 fw-bold"><%= user.username %></h3>
                    <p class="text-secondary mb-4">⭐ Nível <%= user.total %></p>

                    <div class="d-flex justify-content-center social-links">
                        <a href="<%= user.insta %>" class="social-link text-decoration-none">
                            <i class="fab fa-instagram"></i>
                        </a>
                        <a href="<%= user.yt %>" class="social-link text-decoration-none">
                            <i class="fab fa-youtube"></i>
                        </a>
                        <a href="https://wa.me/<%= user.zap %>" class="social-link text-decoration-none">
                            <i class="fab fa-whatsapp"></i>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Formulário de Edição -->
            <div class="col-lg-8">
                <div class="profile-card p-4">
                    <h4 class="fw-bold mb-4">✏️ Editar Perfil</h4>
                    
                    <form id="editForm" action="/editarr/<%= user.username %>" method="post" class="edit-form">
                        <div class="row g-3">
                            <div class="col-12">
                                <label>📷 Foto URL</label>
                                <input type="text" class="form-control" name="ft" value="<%= user.ft %>" placeholder="Insira a URL da sua foto">
                            </div>

                            <div class="col-md-6">
                                <label>🔒 Senha</label>
                                <input type="text" class="form-control" name="password" value="<%= user.password %>" placeholder="Sua senha atual">
                            </div>

                            <div class="col-md-6">
                                <label>📱 WhatsApp</label>
                                <input type="text" class="form-control" name="zap" value="<%= user.zap %>" placeholder="Número com DDD">
                            </div>

                            <div class="col-12">
                                <label>🖼️ Wallpaper URL</label>
                                <input type="text" class="form-control" name="wallpaper" value="<%= user.wallpaper %>" placeholder="URL do wallpaper">
                            </div>

                            <div class="col-md-6">
                                <label>🎥 YouTube</label>
                                <input type="text" class="form-control" name="yt" value="<%= user.yt %>" placeholder="URL do canal">
                            </div>

                            <div class="col-md-6">
                                <label>📸 Instagram</label>
                                <input type="text" class="form-control" name="insta" value="<%= user.insta %>" placeholder="@seu_usuario">
                            </div>

                            <div class="col-12 mt-3">
                                <button type="submit" class="save-btn">
                                    💾 Salvar Alterações
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </main>

    <!-- Scripts Otimizados -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- Script de Redirecionamento -->
    <script>
        document.getElementById('editForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            try {
                const formData = new URLSearchParams(new FormData(e.target));
                const response = await fetch(e.target.action, {
                    method: 'POST',
                    body: formData,
                    headers: {
                        'Content-Type': 'application/x-www-form-urlencoded',
                    },
                });

                if (response.ok) {
                    window.location.href = '/myperfil';
                } else {
                    const error = await response.text();
                    alert(`Erro ao salvar: ${error}`);
                }
            } catch (error) {
                console.error('Erro:', error);
                alert('Erro na comunicação com o servidor');
            }
        });
    </script>
</body>
</html>